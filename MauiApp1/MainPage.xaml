<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.MainPage"
             HideSoftInputOnTapped="True">

    <ScrollView>
        <VerticalStackLayout Padding="30,0"
                             Spacing="25">

            <Label Text="Tapping outside a focused Entry does not remove the cursor from that Entry and does not let the Entry lose focus." />
            <Label Text="It is only possible to switch focus to a different Entry, but it is not possible to unfocus all Entries by tapping somewhere else on the page." />
            <Label Text="I would expect that setting HideSoftInputOnTapped=&quot;True&quot; would allow unfocusing the focused Entry by tapping somewhere on the page. However, it only closes the soft keyboard if that is opened." />

            <Entry />

            <Entry />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
